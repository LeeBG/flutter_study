import 'package:flutter/material.dart';

class ProfileDrawer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: double.infinity, //앱의 최대크기만큼 사이즈가 자동확장
      width: 200,
      color: Colors.blue,
    );
  }
}
